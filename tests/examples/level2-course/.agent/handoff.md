# Handoff — resume snapshot

**Updated:** 2026-08-16. A snapshot to resume from — not a backlog or a research archive. Overwrite as state moves.

## State
- Shell + manifest-driven lesson list: working.
- Lessons authored: `intro` (done). Manifest has `intro` only.
- Progress adapter `store/progress.js`: implemented and used (see decision 0001 — settled, don't re-ask).
- Quiz module `features/quiz.js`: wired for `intro`.

## In flight
- Nothing half-done. Working tree clean at snapshot time.

## Next concrete step
- Author lesson `budgeting`: add one entry to `content/manifest.json` and `content/lessons/budgeting.html`
  (copy `intro.html`'s structure). This is routine content work — **does not need Web Builder.**

## Do NOT re-decide
- Progress storage → decided in `.agent/decisions/0001-progress-storage.md` (localStorage behind the adapter).
</content>
