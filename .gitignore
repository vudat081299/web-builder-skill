.DS_Store
*.log

# Local AI tooling — never part of the project (also covered by global excludes)
.kv/
.remember/

# Build artifacts (packaged skill)
*.zip
*.skill

# Local, per-machine install target for scripts/install-skill.sh (example is committed)
scripts/install-config.local.sh
